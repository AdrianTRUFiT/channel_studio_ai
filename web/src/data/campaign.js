// Single source of truth: the SAME governed campaign file the Phase 01 gate
// validates (data/campaigns/...). The dashboard never invents its own data.
import campaign from "../../../data/campaigns/the-mind-is-a-computer.campaign.json";

export default campaign;
